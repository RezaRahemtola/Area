import 'package:flutter/material.dart';

class activityHero extends StatelessWidget {
  const activityHero({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Activity Page Content');
  }
}
