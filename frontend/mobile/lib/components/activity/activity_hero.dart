import 'package:flutter/material.dart';

class ActivityHero extends StatelessWidget {
  const ActivityHero({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('Activity Page Content');
  }
}
